# Intentionally empty. jquery.i18n.properties always probes "<name>_<lang>.properties"
# in addition to the base file. Since language.js sets name=<lang>, the base file
# (en.properties) already holds the English strings; this stub only exists so the
# library's locale-variant request returns 200 instead of a harmless 404.
